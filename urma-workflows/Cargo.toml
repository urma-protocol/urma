[package]
name = "urma-workflows"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
readme = false
include = ["Cargo.toml", "src/**/*.rs"]
description = "URMA archive and vault workflows"

[dependencies]
urma-io = { version = "0.1.0", path = "../urma-io" }
urma-core = { version = "0.1.0", path = "../urma-core" }
urma-identity = { version = "0.1.0", path = "../urma-identity" }
rand = "0.8"
zeroize = "1"
urma-runtime = { version = "0.1.0", path = "../urma-runtime" }
