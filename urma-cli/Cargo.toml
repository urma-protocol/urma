[package]
name = "urma-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
readme = false
include = ["Cargo.toml", "src/**/*.rs"]
description = "Command-line tools for URMA identities, wallets, Wire, archives and Git"

[[bin]]
name = "urma"
path = "src/main.rs"

[dependencies]
urma-io = { version = "0.1.0", path = "../urma-io" }
bitcoin = { version = "0.32", features = ["rand-std"] }
bitcoincore-rpc = "0.19"
aes = "0.8"
ctr = "0.9"
hmac = "0.12"
clap = { version = "4", features = ["derive"] }
console = "0.15"
ctrlc = "3.4"
dialoguer = "0.11"
indicatif = "0.18"
hex = "0.4"
hkdf = "0.12"
minreq = { version = "2.14", features = ["https-rustls"] }
rand = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tempfile = "3"
url = "2"
zeroize = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", default-features = false, features = ["fmt", "registry"] }

urma-core = { version = "0.1.0", path = "../urma-core" }
urma-profiles = { version = "0.1.0", path = "../urma-profiles" }
urma-identity = { version = "0.1.0", path = "../urma-identity" }
urma-wallet = { version = "0.1.0", path = "../urma-wallet" }
urma-workflows = { version = "0.1.0", path = "../urma-workflows" }

urma-chain = { version = "0.1.0", path = "../urma-chain" }

urma-wire = { version = "0.1.0", path = "../urma-wire" }
urma-runtime = { version = "0.1.0", path = "../urma-runtime" }

urma-files = { version = "0.1.0", path = "../urma-files" }
urma-git = { version = "0.1.0", path = "../urma-git" }

[dev-dependencies]
anyhow = "1"
